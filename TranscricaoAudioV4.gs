/**
 * Entrada pública exclusiva da transcrição por áudio.
 * Mantém um único nome exposto ao front e delega ao motor atual.
 */
function transcreverAudioMp3V4(dados) {
  return transcreverAudioMp3V3(dados);
}
